This folder contains static assets served at the root path.

Replace the SVG placeholders with your real images (JPG/PNG/WebP) using the same filenames if you want to keep existing paths working.

Examples:

- /marble-bg.svg -> replace with your hero background (e.g., marble-bg.jpg)
- /products/ganesh-01.svg -> replace with product photo
